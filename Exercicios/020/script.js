function podeDigigir(idade) {
    if (idade >= 18) {
        return console.log("Pode dirigir");
    } else {
        return console.log("Não pode dirigir");
    }

    return Math.floor(Math.random() * a) + 1;
    //Math.floor serve pra arredondar o numero do metodo random.
}

podeDigigir(20);
podeDigigir(30);
podeDigigir(16);