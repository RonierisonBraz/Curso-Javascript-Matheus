let nome = "joão";

console.log(nome.length);//apos o ponto é uma propriedade que pode ser atribuida a variavel.

let numeros = [1, 23, 34, 5, 6, 7, 8];

console.log(numeros.length);
console.log(numeros['length']);
console.log(numeros[1]);